<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.medease">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedEase">
        <activity
            android:name=".MakeAppointment"
            android:exported="false" />
        <activity
            android:name=".ui.order.OrderStatusFragment"
            android:exported="false" />
        <activity
            android:name=".ui.order.CheckoutFragment"
            android:exported="false" />
        <activity
            android:name=".ui.order.OrderMedsFragment"
            android:exported="false" />

    <activity
        android:name=".DoctorDashboardActivity"
        android:exported="true">
        <!-- INI yang bikin dashboard tampil pertama -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
        android:name=".MainActivity"
        android:exported="false" />

</application>


</manifest>
